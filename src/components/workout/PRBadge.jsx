const PRBadge = ({ label }) => {
  return (
    <span className="ml-2 px-2 py-0.5 text-xs font-semibold text-green-700 bg-green-100 rounded">
      {label}
    </span>
  );
};

export default PRBadge;
